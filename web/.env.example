# Copy to .env.local for local dev against a non-default API port, or set
# NEXT_PUBLIC_API_URL directly in the Vercel project settings for the deployed site.
#
# Local dev default (no file needed) is http://localhost:8000 — see web/lib/api.ts.
NEXT_PUBLIC_API_URL="https://your-finctl-api.onrender.com"

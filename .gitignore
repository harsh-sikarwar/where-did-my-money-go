# --- Secrets: this block exists from commit one, deliberately ---
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# --- Node / Next.js ---
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# --- Generated data & runtime output ---
# Batches are regenerated from a seed; the seed is the source of truth, not the CSVs.
# Generated batches, anywhere in the tree — `data/*` alone only matches the root.
# Batches are reproducible from a seed, so the seed is the artefact worth keeping,
# not several megabytes of CSV.
/data/*
!/data/.gitkeep
engine/data/
*.sqlite
*.sqlite3
*.db
audit/*.jsonl

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
